[package]
name = "plumbline"
version = "0.0.2"
edition = "2021"
description = "Keep a Rust crate's shipped docs in lock-step with its real binary output, and gate cargo publish on it. crates.io is write-once; plumbline is the plumb-line that proves the docs hang true at the publish moment."
license = "Apache-2.0"
repository = "https://github.com/rgfind/plumbline"
keywords = ["cargo", "publish", "docs", "release", "contract"]
categories = ["development-tools::cargo-plugins", "command-line-utilities"]
readme = "README.md"

# The exhaustive list of what ships to crates.io. plumbline reads this same
# array to build its own packaged-file allowlist (gate 4), so Cargo.toml is the
# single source of truth for what leaves the repo. Keep it lean: the binary's
# sources plus the two docs a consumer sees. (Cargo always adds Cargo.toml,
# Cargo.lock, and its vcs-info; those need no listing.)
include = [
    "src/**/*.rs",
    "README.md",
    "LICENSE",
]

# crate name is `plumbline`; the command is `plumb` (as beads_rust ships `br`).
[[bin]]
name = "plumb"
path = "src/main.rs"

[dependencies]
# Config, contract fixtures, and captured envelopes are all JSON. One dependency,
# same as the tool that plumbline grew out of.
serde_json = "1"
sha2 = "0.10"
toml = "0.8"
