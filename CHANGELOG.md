# Changelog

## 0.0.3 — release verification safety

- Add declared local verification gates and exact GitHub Actions proof for release preflight.
- Pin release automation to Rust 1.98.0 and run the declared CI policy.

## 0.0.2 — guarded release command

- Add `plumb release` for checked, annotated, atomic release tagging.
